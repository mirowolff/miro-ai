# {{CUSTOMER_NAME}} Miro Plugin

Custom Miro integration plugin for {{CUSTOMER_NAME}}.

## Overview

This plugin connects Miro with {{CUSTOMER_NAME}}'s tools to enable {{PRIMARY_USE_CASE}}.

**Data Sources:**
{{DATA_SOURCES_LIST}}

**Commands:**
{{COMMANDS_LIST}}

## Setup

### 1. Install Dependencies

Ensure you have Node.js 18+ installed.

### 2. Configure Credentials

This plugin requires API credentials for the connected services. Set these environment variables:

{{CREDENTIALS_SETUP}}

### 3. Enable the Plugin

Add this plugin to your Claude Code configuration:

```bash
# From the plugin directory
claude plugins add .
```

Or add to your `.claude/settings.json`:

```json
{
  "plugins": [
    "./path/to/{{CUSTOMER_SLUG}}-miro-plugin"
  ]
}
```

## Commands

{{COMMANDS_DOCUMENTATION}}

## MCP Configuration

This plugin uses the following MCP servers:

```json
{{MCP_CONFIG}}
```

## Usage Examples

### Sync Data to Miro
```
/{{CUSTOMER_SLUG}}:sync https://miro.com/app/board/your-board-id=
```

### Create Visualizations
```
/{{CUSTOMER_SLUG}}:visualize https://miro.com/app/board/your-board-id= flowchart
```

### Export from Miro
```
/{{CUSTOMER_SLUG}}:export https://miro.com/app/board/your-board-id=
```

## Customization

### Adding New Commands

Create new `.md` files in the `commands/` directory following the existing patterns.

### Modifying Data Mappings

Edit the command files to adjust how data is transformed between Miro and your systems.

### Adding Data Sources

1. Research the MCP server for your tool
2. Add configuration to `.mcp.json`
3. Create commands that use the new MCP

## Troubleshooting

### Credentials Not Found
Ensure environment variables are set correctly. Check with:
```bash
echo $MIRO_OAUTH_TOKEN
```

### MCP Connection Failed
Verify the MCP server is installed:
```bash
npx -y @anthropic/miro-mcp --version
```

### Board Access Denied
Ensure the Miro OAuth token has access to the board you're trying to use.

## Support

For issues with this plugin, contact your Solution Architect.

For Miro platform questions, see the [Miro Developer Documentation](https://developers.miro.com/).

---

*Generated by miro-solutions plugin for Claude Code*
